SELECT * FROM `cours` WHERE level = "Intermediate" and title REGEXP '^101'
ORDER BY title
    //if(post[submit]){
    //insert course
    //last id
    // loop sections = section
    // insert section + last id
//}